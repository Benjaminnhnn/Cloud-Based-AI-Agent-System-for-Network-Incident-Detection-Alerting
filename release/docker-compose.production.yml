version: "3.9"

services:
  redis:
    image: redis:7-alpine
    container_name: redis-prod
    restart: always
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data-prod:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - aiops-network

  redis-cache:
    image: redis:7-alpine
    container_name: redis-cache-prod
    restart: always
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-cache-data-prod:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
    networks:
      - aiops-network

  redis-exporter:
    image: oliver006/redis_exporter:v1.67.0
    container_name: redis-exporter-prod
    restart: always
    environment:
      REDIS_ADDR: redis://redis-cache:6379
    ports:
      - "9121:9121"
    networks:
      - aiops-network
    depends_on:
      redis-cache:
        condition: service_healthy

  ai-agent:
    image: ghcr.io/${GHCR_OWNER:-your-org}/aws-hybrid-ai-agent:${IMAGE_TAG:-latest}
    container_name: ai-agent-prod
    restart: always
    env_file:
      - .env.production
    environment:
      AI_AGENT_PORT: "8000"
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      PYTHONUNBUFFERED: "1"
    ports:
      - "0.0.0.0:8000:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health').read()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - aiops-network
    depends_on:
      redis:
        condition: service_healthy

  celery-worker:
    image: ghcr.io/${GHCR_OWNER:-your-org}/aws-hybrid-ai-agent:${IMAGE_TAG:-latest}
    container_name: celery-worker-prod
    restart: always
    env_file:
      - .env.production
    environment:
      REDIS_HOST: redis
      REDIS_PORT: "6379"
      REDIS_DB: "0"
      PYTHONUNBUFFERED: "1"
    command: ["celery", "-A", "core.celery_app.celery_app", "worker", "--loglevel=INFO"]
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - aiops-network

  log-watcher:
    image: ghcr.io/${GHCR_OWNER:-your-org}/aws-hybrid-ai-agent:${IMAGE_TAG:-latest}
    container_name: log-watcher-prod
    restart: always
    env_file:
      - .env.production
    environment:
      WEBHOOK_URL: http://ai-agent:8000/webhook
      LOG_FILES: /var/log/syslog,/var/log/cloud-init.log,/var/log/dnf.log,/tmp/test_syslog.log
      PYTHONUNBUFFERED: "1"
    command: ["sh", "-c", "sed -i 's|WEBHOOK_URL = \"http://localhost:8000/webhook\"|WEBHOOK_URL = \"http://ai-agent:8000/webhook\"|' log_watcher.py && python log_watcher.py"]
    volumes:
      - /var/log:/var/log:ro
      - /tmp/aiops-test-syslog.log:/tmp/test_syslog.log:ro
    depends_on:
      ai-agent:
        condition: service_healthy
    networks:
      - aiops-network

  payment-api:
    image: ghcr.io/${GHCR_OWNER:-your-org}/aws-hybrid-payment-api:${IMAGE_TAG:-latest}
    container_name: payment-api-prod
    restart: always
    env_file:
      - .env.production
    environment:
      ENVIRONMENT: production
      DATABASE_URL: postgresql://aiops_user:aiops_pass@postgres:5432/aiops_db
    ports:
      - "8080:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health').read()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
    networks:
      - aiops-network
    depends_on:
      postgres:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    container_name: postgres-prod
    restart: always
    environment:
      POSTGRES_USER: aiops_user
      POSTGRES_PASSWORD: aiops_pass
      POSTGRES_DB: aiops_db
    volumes:
      - postgres-data-prod:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
      - ./database/seed.sql:/docker-entrypoint-initdb.d/02-seed.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aiops_user -d aiops_db"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - aiops-network

  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:v0.15.0
    container_name: postgres-exporter-prod
    restart: always
    environment:
      DATA_SOURCE_NAME: postgresql://aiops_user:aiops_pass@postgres:5432/aiops_db?sslmode=disable
    ports:
      - "9187:9187"
    networks:
      - aiops-network
    depends_on:
      postgres:
        condition: service_healthy

  frontend-web:
    image: ghcr.io/${GHCR_OWNER:-your-org}/aws-hybrid-frontend:${IMAGE_TAG:-latest}
    container_name: frontend-web-prod
    restart: always
    ports:
      - "80:80"
    environment:
      PAYMENT_API_UPSTREAM: ${PAYMENT_API_UPSTREAM:-http://payment-api:8000}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - aiops-network

networks:
  aiops-network:
    driver: bridge

volumes:
  redis-data-prod:
  redis-cache-data-prod:
  postgres-data-prod:
