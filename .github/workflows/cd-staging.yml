name: CD Staging

on:
  push:
    branches: [develop]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    outputs:
      owner_lc: ${{ steps.vars.outputs.owner_lc }}
      image_tag: ${{ steps.vars.outputs.image_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare image variables
        id: vars
        run: |
          echo "owner_lc=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"
          echo "image_tag=staging-${GITHUB_SHA}" >> "$GITHUB_OUTPUT"

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build & push AI Agent
        uses: docker/build-push-action@v6
        with:
          context: ./agent_src
          push: true
          tags: |
            ghcr.io/${{ steps.vars.outputs.owner_lc }}/aws-hybrid-ai-agent:${{ steps.vars.outputs.image_tag }}
            ghcr.io/${{ steps.vars.outputs.owner_lc }}/aws-hybrid-ai-agent:staging-latest

      - name: Build & push Payment API
        uses: docker/build-push-action@v6
        with:
          context: ./demo-web/backend
          push: true
          tags: |
            ghcr.io/${{ steps.vars.outputs.owner_lc }}/aws-hybrid-payment-api:${{ steps.vars.outputs.image_tag }}
            ghcr.io/${{ steps.vars.outputs.owner_lc }}/aws-hybrid-payment-api:staging-latest

  deploy-staging:
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: staging
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare environment files
        run: |
          cp release/.env.example release/.env.staging
          cp release/.env.example release/.env.production

      - name: Preflight SSH connectivity
        env:
          SSH_HOST: ${{ secrets.SSH_HOST }}
          SSH_PORT: ${{ secrets.SSH_PORT }}
        run: |
          set -euo pipefail
          echo "Checking SSH target ${SSH_HOST}:${SSH_PORT}"

          if [[ "$SSH_HOST" =~ ^10\.|^127\.|^192\.168\.|^172\.(1[6-9]|2[0-9]|3[0-1])\. ]]; then
            echo "::error::SSH_HOST appears to be a private IP. Use EC2 Public IPv4 for GitHub-hosted runners."
            exit 1
          fi

          if ! timeout 10 bash -c "</dev/tcp/${SSH_HOST}/${SSH_PORT}"; then
            echo "::error::Cannot reach ${SSH_HOST}:${SSH_PORT} from GitHub runner (network timeout)."
            echo "::error::Check EC2 Security Group inbound rule for SSH (port 22) and NACL/route settings."
            exit 1
          fi

          echo "SSH target is reachable from runner."

      - name: Setup EC2 directory and install Docker
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script_stop: true
          timeout: 120s
          command_timeout: 120s
          script: |
            mkdir -p /opt/aws-hybrid/{release,automation}
            echo "Directories created"

      - name: Install Docker
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script_stop: true
          timeout: 180s
          command_timeout: 180s
          script: |
            if ! command -v docker &> /dev/null; then
              echo "Installing Docker..."
              sudo yum update -y
              sudo yum install docker -y
              sudo systemctl start docker
              sudo systemctl enable docker
              sudo usermod -aG docker ec2-user
              echo "Docker installed and started"
            else
              echo "Docker already installed"
            fi

      - name: Install Docker Compose (if needed)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script_stop: true
          timeout: 120s
          command_timeout: 120s
          script: |
            if ! command -v docker-compose &> /dev/null; then
              echo "Installing Docker Compose..."
              sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
              sudo chmod +x /usr/local/bin/docker-compose
              echo "Docker Compose installed"
            else
              echo "Docker Compose already installed"
            fi

      - name: Copy files to EC2 using SCP
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          scp -o StrictHostKeyChecking=no -o ConnectTimeout=10 -i ~/.ssh/deploy_key -r release/ ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/opt/aws-hybrid/
          scp -o StrictHostKeyChecking=no -o ConnectTimeout=10 -i ~/.ssh/deploy_key -r automation/ ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:/opt/aws-hybrid/
          rm ~/.ssh/deploy_key

      - name: Run deploy script on EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT }}
          script_stop: true
          timeout: 300s
          command_timeout: 300s
          script: |
            cd /opt/aws-hybrid
            chmod +x automation/deploy.sh
            export GHCR_USERNAME="${{ secrets.GHCR_USERNAME }}"
            export GHCR_TOKEN="${{ secrets.GHCR_TOKEN }}"
            export GHCR_OWNER="${{ needs.build-and-push.outputs.owner_lc }}"
            ./automation/deploy.sh staging "${{ needs.build-and-push.outputs.image_tag }}"
